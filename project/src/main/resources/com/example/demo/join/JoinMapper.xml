<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.join.JoinMapper">
	 <insert id="saveUser" parameterType="com.example.demo.user.User">
	 	insert into user(userName, userDate, userId, userPass)
	 	values (#{userName}, #{userDate}, #{userId}, #{userPass})
	 </insert>
</mapper>